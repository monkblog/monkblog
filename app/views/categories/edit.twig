{% extends 'admin.layout' %}

{% block content %}
    {{ form_model( category, { 'route' : { 0 : 'admin.categories.update', 1 : category.id }, 'method' : 'put' } ) }}
    {{ form_label( 'title', 'Title' ) }}
    {{ form_text( 'title' ) }}
    {{ form_label( 'slug', 'Slug' ) }}
    {{ form_text( 'slug' ) }}
    {{ form_label( 'description', 'Description' ) }}
    {{ form_text( 'description' ) }}
    {{ form_submit( 'Save', { 'class' : 'button' } ) }}
    {{ form_close() }}
{% endblock %}
