{% extends 'admin.layout' %}

{% block content %}
    {{ form_model( category, { 'route' : 'admin.categories.store' } ) }}
    {{ form_label( 'title', 'Title' ) }}
    {{ form_text( 'title' ) }}
    {{ form_label( 'slug', 'Slug' ) }}
    {{ form_text( 'slug' ) }}
    {{ form_label( 'description', 'Description' ) }}
    {{ form_text( 'description' ) }}
    {{ form_submit( 'Create', { 'class' : 'button' } ) }}
    {{ form_close() }}
{% endblock %}
