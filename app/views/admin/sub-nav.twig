<dl class="sub-nav">
  <dt>{{ label }}</dt>
{% for count, tab in tabs %}
    {% set active = '' %}
    {% if slug is defined and slug == tab.slug and not activeSet %}
      {% set active = ' class="active"' %}
    {% endif %}
    <dd{{ active }}><a href="{{ url_route( 'admin.options.show', tab.slug ) }}">{{ tab.display_name }}</a></dd>
{% endfor %}
</dl>