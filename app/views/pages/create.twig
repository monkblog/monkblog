{% extends 'admin.layout' %}

{% block content %}
    {{ form_model( page, { route : 'admin.pages.store' } ) }}
    {{ form_label( 'title', 'Title' ) }}
    {{ form_text( 'title' ) }}
    {{ form_label( 'slug', 'Slug' ) }}
    {{ form_text( 'slug' ) }}
    {{ form_label( 'body', 'Body' ) }}
    {{ form_textarea( 'body' ) }}
    {{ form_submit( 'Save', { class : 'button' } ) }}
    {{ form_close() }}
{% endblock %}
