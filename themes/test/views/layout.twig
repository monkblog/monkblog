<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ ( pageTitle is not empty ? pageTitle : 'MonkBlog' ) }}{{ ( siteTitle is not empty ? ' | ' ~ siteTitle : '' ) }}</title>
        <!--[if lte IE 9]>
        <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->
        {% if logged_in == true %}
        <link type="text/css" rel="stylesheet" href="{{ elixir( 'assets/css/admin.css' ) }}">
        {% endif %}
    </head>
    <body>
        {% if logged_in == true %}
            {% include 'admin::nav' %}
        {% endif %}
        <div class="row">
            <header>
                <h1><a href="{{ url_route( 'home' ) }}">{{ siteTitle }}</a> <small>{{ tagline }}</small></h1>
            </header>
        </div>
        <div>
            <div class="small-12 columns">
                {% block top_content %}{% endblock %}
            </div>
        </div>
        <div>
            <div>
                {% block content %}{% endblock %}
            </div>
            <div>
                {% include 'test::sidebar' %}
            </div>
        </div>
        <div>
            <footer class="small-12 columns">
                <p>This site is an online journal. It's not really intended to be copyrighted, trademarked, or otherwise legal-ified. I trust that you won't take my writing and use it as your own. If you do, though, I'm not going to do anything about it.</p>
            </footer>
        </div>
    </body>
</html>
