<!DOCTYPE html>
<html>
    <head>
        <title>{{ pageTitle }} | {{ siteTitle }}</title>
        <link href='http://fonts.googleapis.com/css?family=Cabin:500|Lora:400,700,400italic,700italic' rel='stylesheet' type='text/css'>
        <link type="text/css" rel="stylesheet" href="{{ theme_asset( 'css/main.css' ) }}">
    </head>
    <body class="{% block bodyclass %}{% endblock %}">
        <div class="row">
            <header class="small-12 columns">
                <h1><a href="{{ url_route( 'home' ) }}">{{ siteTitle }}</a> <small>{{ tagline }}</small></h1>
            </header>
        </div>
        <div class="row">
            <div class="small-12 columns">
                {% block top_content %}{% endblock %}
            </div>
        </div>
        <div class="row">
            <div class="small-9 columns">
                {% block content %}{% endblock %}
            </div>
            <div class="small-3 columns">
                {% include 'monk-blog::sidebar' %}
            </div>
        </div>
        <div class="row">
            <footer class="small-12 columns">
                <p>This site is an online journal. It's not really intended to be copyrighted, trademarked, or otherwise legal-ified. I trust that you won't take my writing and use it as your own. If you do, though, I'm not going to do anything about it.</p>
            </footer>
        </div>
        <script src="//code.jquery.com/jquery-2.1.3.min.js"></script>
        <script>window.jQuery || document.write('<script src="{{ theme_asset( 'vendor/jquery/dist/jquery.min.js' ) }}"><\/script>')</script>
        <script src="{{ theme_asset( 'vendor/foundation/js/foundation.min.js' ) }}"></script>
        <script src="{{ theme_asset( 'js/app.js' ) }}"></script>
    </body>
</html>
